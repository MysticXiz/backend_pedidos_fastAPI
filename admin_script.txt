@auth_router.post("/me")
async def c_adm(session: Session = Depends(get_session)):
    email = 'admin@'
    password= 'admin123'
    name='admin'
    password_hashed = argon2.hash(password)

    new_user = User(
        name=(name or "").strip(),
        email=email,
        password=password_hashed,
        active=True,
        admin=True  # define como admin
    )
    session.add(new_user)
    session.commit()
    return {"message": "Usuário admin criado com sucesso"}